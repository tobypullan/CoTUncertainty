# Runtime dependencies for CoT experiments.
# Install with:
#   python -m pip install -r requirements.txt

# Core model stack (Gemma 3 compatible)
torch==2.7.0
transformers==5.1.0
datasets==4.5.0
accelerate==1.2.1
huggingface_hub==1.4.1
tokenizers==0.22.2
safetensors==0.7.0

# Compatibility fixes for first-run issues:
# - transformers chat templates require jinja2>=3.1
# - Gemma3 import path needs Pillow with Image.Resampling
jinja2==3.1.6
pillow==12.1.0

# Plotting + numeric utilities used by scripts
matplotlib==3.5.1
numpy==1.21.5

# API client used by OpenRouter-backed experiment scripts
requests==2.32.5
